## Getting Started

Install the dependencies and run the development server

```bash
yarn install
yarn dev
```

To deploy on production, first need to build:

```bash
yarn build
```

Then:

```bash
yarn start
```
